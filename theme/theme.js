import { mode } from "@chakra-ui/theme-tools";


export const theme = {
    config: {
        initialColorMode: 'light',
    },
    styles: {
        global: (props) => ({
            body: {
                background: 'gray.200',
            },
        })
    },
    fonts: {
        'numbers': 'Space Mono, monospace'
    },
    colors: {
    }
}
